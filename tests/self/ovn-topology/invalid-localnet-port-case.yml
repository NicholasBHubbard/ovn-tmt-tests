- name: Require rejection of {{ ovn_localnet_port_negative_case.name }}
  block:
    - name: Apply invalid localnet port configuration
      ansible.builtin.include_role:
        name: ovn_localnet_ports
      vars:
        ovn_localnet_ports: "{{ ovn_localnet_port_negative_case.localnet_ports }}"

    - name: Fail if invalid configuration was accepted
      ansible.builtin.fail:
        msg: >-
          Invalid configuration was accepted:
          {{ ovn_localnet_port_negative_case.name }}
  rescue:
    - name: Verify expected validation failure
      ansible.builtin.assert:
        that:
          - >-
            ansible_failed_result.msg
            == 'Localnet port configuration is invalid.'
            or
            ansible_failed_result.results | default([])
            | selectattr(
                'msg',
                'equalto',
                'Localnet port configuration is invalid.'
              )
            | list | length == 1
        fail_msg: >-
          {{ ovn_localnet_port_negative_case.name }} failed unexpectedly:
          {{ ansible_failed_result.msg }}
