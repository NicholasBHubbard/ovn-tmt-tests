- name: Reject invalid OVN chassis configuration
  hosts: all
  become: true
  roles:
    - ovn_chassis
  vars:
    ovn_chassis_name: "{{ ovn_chassis_invalid_cases[ovn_chassis_invalid_case] }}"
    ovn_chassis_invalid_cases:
      invalid_name: invalid name
