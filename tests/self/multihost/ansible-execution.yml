- name: Verify test-scoped Ansible execution
  hosts: all
  gather_facts: false
  tasks:
    - name: Confirm test-scoped Ansible execution reaches each guest
      ansible.builtin.assert:
        that:
          - inventory_hostname in groups['all']
