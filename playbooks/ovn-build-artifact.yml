- name: Build OVN source artifact
  hosts: "{{ ovn_artifact_builder_hosts | default('central') }}"
  become: true
  roles:
    - role: ovn_artifact
      ovn_artifact_action: build
      when: ovn_artifact_enabled | default(true) | bool
