summary: OVN unit tests with gcc and jemalloc

environment+:
  OVN_CONFIGURE_FLAGS: "LIBS=-ljemalloc"

provision:
  - how: container
    image: fedora

prepare+<:
  - how: shell
    script: dnf install -y jemalloc-devel

prepare+:
  - how: ansible
    playbook: playbooks/ovn-unit-test-deps.yml

discover:
  how: fmf
  test:
    - /tests/ovn-make-check
